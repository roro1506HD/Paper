name: Paper-Test-Plugin
version: ${version}
main: io.papermc.testplugin.TestPlugin
description: Paper Test Plugin
author: PaperMC
api-version: ${apiversion}

commands:
  run-register-unregister-by-one:
  run-register-unregister-at-once:
  reregister-this:
